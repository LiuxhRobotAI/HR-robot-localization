# # ! DO NOT MANUALLY INVOKE THIS setup.py, PLEASE USE CATKIN INSTEAD
# from distutils.core import setup # prefer not to use setuptools as it will add files in src
# from catkin_pkg.python_setup import generate_distutils_setup

# d = generate_distutils_setup(
#     packages=['third_party'], # Python package name, here is 'third_party', where your xxx.py (modules) are
#     package_dir={'': 'include'} # path of the 'third_party' Python package or put it in the same path of setup.py
# )

# # call setup of 'distutils' package
# setup(**d)
